# Screenshots

Images referenced by the root `README.md`.

## Present

| File | Shows | Used as |
|---|---|---|
| `menu.png` | All-in-one menu (`Ctrl+Shift+K`) | Hero |
| `editor.jpg` | Annotation editor on a photo | Gallery |
| `history.png` | History grid + filter bar (dark) | Gallery |
| `viewer.jpg` | Image viewer with the action bar | Gallery |
| `recording.png` | Floating recording bar over a selection | Gallery |
| `settings.png` | Settings — theme switcher (dark) | Gallery |
| `settings-light.png` | Settings — theme switcher (light) | Gallery |

The gallery is an HTML `<table>` with `width="50%"` cells + `width="100%"`
images, so every screenshot renders the same width (a markdown table sizes each
image to its natural width, which looked uneven).

## Still worth adding (not captured yet)

- **Region-capture hero** — the selection overlay mid-drag with the **magnifier
  loupe + dimensions badge**. The signature shot; would make the best hero (a
  short GIF of capture → post-capture panel → copy is even better).

Tip: capture on a clean wallpaper at 100% scale, crop tight, keep one theme
(dark reads most "product"), and aim for < ~300 KB per still.
